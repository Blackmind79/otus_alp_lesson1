# Administrator Linux Professional

[[_TOC_]]

## Описание

### С чего начинается Linux

Цели занятия: узнать, как работает ядро, как программы взаимодействуют с ядром;

### Краткое содержание

- краткая история Unix и Linux
- краткий обзор архитектуры x86 в свете операционных систем
- многозадачность и её реализация в Linux
- системные вызовы и сигналы
- версии Linux
- сборка ядра.

## Домашнее задание

Обновление ядра системы

Цель: научиться обновлять ядро в ОС Linux;

Описание/Пошаговая инструкция выполнения домашнего задания:

🎯Задание:

- Запустите ВМ c Ubuntu
- Обновите ядро ОС на новейшую стабильную версию из mainline-репозитория
- Оформите отчет в README-файле в GitHub-репозитории

⭐️Задание со звездочкой

- Собрать ядро самостоятельно из исходных кодов

## Шаги выполнения

Создано два скрипта - обновление ядра для дистрибутива mainline и кастомная сборка.

### Mainline

Скрипт `mainline.sh`.
Для обновления ядра на проверенную версию заходим на сайт [Ubuntu mainline](https://kernel.ubuntu.com/mainline) и смотрим нужную версию ядра.

Версия будет ниже, чем на kernel.org, так как тут она проверяется тестами для дистрибутива.

После чего в скрипте указываем версию в переменную `KERNEL`. Для удобства папку загрузки пакетов также вынес в `DOWNLOAD_DIR`.
Далее запустите скрипт:

```bash
sudo chmod 700 ./mainline.sh && ./mainline.sh
```

После перезагрузки система запустится с новым ядром.

**ВНИМАНИЕ! Я не делал подпись ядра, так что нужно в настройках VM отключить SecureBoot!**

### Кастомная сборка

Все действия занес в скрипт `make_new_core.sh`.

```bash
sudo chmod 700 ./make_new_core.sh && ./make_new_core.sh
```

Краткие комментарии:

- для удобства определяю переменную `KERNEL_FN`, где прописываю версию ядра
- подразумеваю что домашняя папка аккаунта находится в `/home`.
- для `wget` использую ключ `--directory-prefix` с абсолютным путем, так как бывают ошибки при работе с относительным путем
- можно было унифицировать выбор ядра на последнюю версию, но смысла нет

ВНИМАНИЕ! Не подписываю сертификатами, так что отключите Secure Boot в вашем конфиге, который копируете!
Для этого в сборке отключаю (безопасно через скрипт `scripts/config`):

```bash
scripts/config --disable SYSTEM_TRUSTED_KEYS
scripts/config --disable BUILT_IN_CERTIFICATE
scripts/config --disable MODULE_SIG
scripts/config --disable MODULE_SIG_FORCE
scripts/config --disable MODULE_SIG_ALL
scripts/config --disable MODULE_SIG_SHA1
scripts/config --disable MODULE_SIG_SHA256
scripts/config --disable MODULE_SIG_SHA384
scripts/config --disable MODULE_SIG_SHA512
scripts/config --disable MODULE_SIG_SHA3_256
scripts/config --disable MODULE_SIG_SHA3_384
scripts/config --disable MODULE_SIG_SHA3_512
scripts/config --disable MODULE_SIG_KEY_TYPE_RSA
scripts/config --disable MODULE_SIG_KEY_TYPE_ECDSA
```
